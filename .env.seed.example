# Usado apenas pelo script scripts/seed-test-data.mjs, roda só localmente.
# NUNCA comitar o arquivo .env.seed.local preenchido, nunca usar a service_role
# key no frontend, e nunca rodar este script contra um projeto de produção
# com dados reais.

SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
